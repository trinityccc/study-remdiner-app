server:
  port: ${PORT:8080}

spring:
  application:
    name: study-reminder-backend
  jackson:
    serialization:
      write-dates-as-timestamps: false

supabase:
  url: ${SUPABASE_URL}
  anon-key: ${SUPABASE_ANON_KEY}

app:
  cors:
    allowed-origins: ${FRONTEND_ORIGIN:http://localhost:5173,http://localhost:8080}

